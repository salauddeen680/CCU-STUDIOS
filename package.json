import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "CCU Studios",
  description: "Cosmic Cinematic Universe",
  manifest: "/manifest.json", // <--- BAS YEH EK LINE ADD KARNI HAI
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head />
      <body>{children}</body>
    </html>
  );
}
